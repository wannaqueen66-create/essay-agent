# esag 控制台与升级指南

## 打开与迁移

系统要求：Ubuntu / Debian、Python 3.10+、systemd、root 或 sudo 权限。安装目录为 `/opt/essay-agent`。

已有新版：

```bash
sudo esag
```

首次安装或从旧 Bash 控制台迁移：

```bash
curl -fsSL https://raw.githubusercontent.com/wannaqueen66-create/essay-agent/main/esag | sudo bash
```

选择 `1`。已有完整安装会使用事务更新器，保留 `.env`、`config.yaml`、数据库、报告、接口档案和定时器设置。旧版本第一次迁移的程序快照也可回退；回退到旧 Bash 版本后，使用同一远程入口再次升级。

首次安装完成依赖安装后进入 AI 配置。验证并保存成功才自动启用每日任务；取消时保留已安装文件和管理命令，稍后可在菜单配置。配置完成后到“抓取与报告设置 → 启用 / 停用定时任务”启用任务。

`deploy.sh` 可重复执行，完整安装会走保留数据的更新流程。它不再清空安装目录。发现无可用 Python 环境的不完整安装时会停止并保留文件，而不是删除后重装。

## 主菜单

| 编号 | 功能 |
|---|---|
| 1 | AI 接口与模型 |
| 2 | 抓取与报告设置：单独修改参数、时间及定时任务开关 |
| 3 | 立即运行一次：通过 systemd 执行，日志统一进入 journal |
| 4 | 邮件配置 |
| 5 | 数据源 / CORE |
| 6 | 目标期刊管理 |
| 7 | 最近 200 条服务日志 |
| 8 | 数据库总览 |
| 9 | 待展示池 |
| 10 | 发送测试邮件（先询问） |
| 11 | 更新 / 升级 |
| 12 | 备份 / 恢复配置和数据库 |
| 13 | 在编辑器中编辑 config.yaml 草稿，校验后保存 |
| 14 | 在编辑器中编辑 .env 草稿，校验后保存 |
| 15 | 重新配置 AI：保留论文数据 |
| 16 | 卸载，输入 yes 确认；会删除安装目录内数据和备份 |
| 0 | 退出 |

首次安装的默认值为：最近 1 天、每个检索式每个来源最多 10 篇、相关性阈值 60、Markdown 10 篇、邮件正文 5 篇、待展示池 7 天、输出保留 30 天。邮件默认关闭。每天 07:00 按服务器时区执行，最多随机延迟 5 分钟。

服务为 `oneshot`，运行完成显示 `inactive` 是正常的；同时检查 timer 和上次 Result。

## AI 接口与模型

| 编号 | 操作 |
|---|---|
| 1 | 配置接口名称、Base URL、API Key |
| 2 | 同步模型列表 |
| 3 | 选择主模型 |
| 4 | 设置或关闭备用模型 |
| 5 | 测试 AI 配置 |
| 6 | 切换已保存接口 |
| 7 | 超时与重试 |
| 0 | 返回 |

Key 隐藏输入，编辑时回车保留原值；状态区仅显示末尾 4 个字符。Base URL 使用接口根地址，例如 `https://example.com/custom/v1`，不要填写 `/models` 或 `/chat/completions`。自定义路径会被保留。

模型列表支持搜索、15 项分页、编号选择和完整 ID 输入。输入无效编号不会改变模型。使用 `m` 可手动输入列表外模型；备用模型选择页输入 `off` 可关闭备用。接口不支持 `/models` 时仍可手动输入模型并测试。

同步支持 `next`、`next_page_url`、HTTP Link next 以及 `has_more`/`last_id` 游标。单页列表不截断；分页异常或网络失败不会覆盖旧缓存。供应商使用其他私有分页协议时，需要适配其返回格式。缓存按地址与密钥隔离；进入选择页时缓存超过 24 小时会尝试同步。新增和未再返回的模型可查看，当前主备模型不会自动替换。列表存在不等于可调用，列表消失也不直接判为停用。

选好后使用内置短摘要测试主模型和已配置备用模型。测试调用同一个 `analyze_paper`，检查 JSON、必需字段和 0–100 整数评分；每个模型仅调用一次，不抓论文、不写论文库、不发送邮件。测试前会说明产生少量 API 调用费用。主备模型全部通过后才能保存启用；取消或失败保留旧配置。

日常论文分析使用配置的超时（默认 60 秒）与尝试次数（默认每模型 3 次）。SDK 自身重复重试关闭，避免叠加。主模型失败后才尝试当前接口的备用模型，日志、Excel 和 Markdown 记录实际使用模型和切换原因。跨接口自动故障切换尚未提供。

切换新接口时，旧接口档案保留。已有 `.env` 自动读取，仍是运行时配置来源；手动编辑接口参数会使不匹配的历史验证状态失效。新设置对下一次任务生效，运行中的任务保持启动时配置。正常模型切换不强制重算已缓存的论文。

## 更新与回退

主菜单 `11`：

1. **一键更新全部**：程序、交互脚本和依赖。
2. **仅更新交互脚本**：`esag` 及配套模块；主程序保持原版本。依赖或模块契约不兼容时会要求完整更新。
3. **检查更新**：显示本地程序版本、控制台版本、远程版本和提交说明，不修改安装内容。
4. **回退上次程序版本**：恢复代码与相应依赖，保留当前配置和论文数据。

更新前获取 Git 仓库并固定 commit；检查文件内容摘要、Python/Bash 语法与必要文件。摘要用于发现下载后文件被改动，不等于发布者签名认证。

更新遇到论文任务运行会停止；定时器在应用阶段暂时暂停，完成或失败后恢复原运行状态。更新器为新依赖创建独立虚拟环境，旧环境保留。代码替换前创建快照，导入检查失败或 Ctrl+C 会恢复；成功后重新打开控制台。进程被强制杀死或服务器断电不在自动回退保证范围内。

保留 `.env`、`config.yaml`、`.ai_profiles.json`、数据库、报告、模型缓存、备份和已有定时服务设置；不会将仓库默认定时器覆盖到现有设置。旧定时器未启用时，更新也不会自动启用。

程序快照位于 `.updates/`，依赖环境位于 `.venvs/`。这些为了回退而保留，可能占用额外磁盘；不要删除仍需用于回退的目录。配置/数据库备份位于 `backups/`，与程序回退分开。恢复配置/数据库前会额外创建一份当前备份。

更新过程只从 `main` 获取代码；开发分支需合并后才会出现在正式更新入口。

## 开发验证

```bash
python -m pip install -r requirements.txt
bash -n esag deploy.sh
python -m unittest discover -s tests -v
```

测试使用临时文件、模拟 systemd/依赖安装以及本地 HTTP 模拟接口，不使用真实 API Key，不发送邮件。涵盖模型分页与同步异常、特殊字符配置、主备分析、配置迁移、删除期刊、运行锁、更新失败和中断回退、依赖环境回退及数据保留。
